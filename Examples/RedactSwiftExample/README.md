# Redact Swift Example

A tiny SwiftUI app for trying Redact on Apple platforms.

## Run

Open `RedactExample.xcodeproj` in Xcode, choose a simulator or device, and press Run.

The first redaction downloads the pinned Core ML model to the app cache. Later runs use the cached model offline.
